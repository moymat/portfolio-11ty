<section id="profile">
	<div class="section-title hidden">
		<span class="section-number">02</span>
		<svg preserveAspectRatio="xMinYMin" viewbox="0 0 1100 145" class="section-title-svg">
			<text x="0" y="125" class="section-title-text">Qui suis-je ?</text>
		</svg>
	</div>
	<div class="profile-content">
		<div class="profile-content-left">
			<p class="profile-description hidden">
				Développeur fullstack à <a class="content-link" href="https://galadrim.fr/" target="_blank">Galadrim</a> depuis mars 2022, autodidacte depuis fin 2020, j'ai obtenu le Titre Professionnel Développeur Web et Web Mobile en février 2022 suite à une formation dans l'école O'Clock.
				<br />
				<br />
				Travaillant principalement avec l'environnement Node.js/Javascript, je cherche continuellement a développer mes compétences dans ce domaine tout en expérimentant avec d'autres langages et technologies. 
				<br />
				<br />
				Je suis passionné par le développement et l'opportunité que cette pratique me donne de créer mais aussi de découvrir et d'apprendre de nouvelles manières de faire.
			</p>
			<div class="profile-formation">
				<h4 class="profile-title hidden">expériences</h4>
				<ul class="formation-list hidden">
					{%- for item in metadata.experiences %}
						<li class="formation-item">
							<div class="formation-title" style="--idx: {{loop.index}}">
							<span class="formation-date">{{ item.start }} - {{ item.end }}</span>
							</div>
							<div class="formation-name" style="--idx: {{loop.index}}">
								<a class="formation-link" href="{{ item.link }}" target="_blank" >
								{{ item.employer }} - {{ item.title }}
								</a>
							</div>
						</li>
					{%- endfor %}
				</ul>
			</div>
			<div class="profile-formation">
				<h4 class="profile-title hidden">formations</h4>
				<ul class="formation-list hidden">
					{%- for item in metadata.education %}
						<li class="formation-item">
							<div class="formation-title" style="--idx: {{loop.index}}">
								<span class="formation-date">{{ item.year }}</span
								><span class="formation-school">{{ item.school }}</span>
							</div>
							<div class="formation-name" style="--idx: {{loop.index}}">
								<a class="formation-link" href="{{ item.link }}" target="_blank">{{ item.name }}</a >
							</div >
						</li>
					{%- endfor %}
				</ul>
				<a href="/assets/Deckert-Michel-CV.pdf" target="_blank" class="page-btn hidden">Télécharger mon CV</a>
			</div>
		</div>
		<div class="profile-content-right">
			<div class="profile-img-container hidden">
				<img class="profile-img" src="/assets/img/michel-deckert.webp" alt="Michel Deckert" />
			</div>
			<div class="profile-languages">
				<h4 class="profile-title languages-title hidden">Langages & outils</h4>
				<ul class="languages-list hidden">
					{%- for language in metadata.languages %}
						<li class="language-item" style="--idx: {{loop.index}}">
							<a
								class="{{ metadata.icons[language.name].icon }} colored"
								data-name="{{ metadata.icons[language.name].name }}"
								href="{{ metadata.icons[language.name].link }}"
								target="_blank"
							></a>
						</li>
					{%- endfor %}
				</ul>
			</div>
		</div>
	</div>
</section>
